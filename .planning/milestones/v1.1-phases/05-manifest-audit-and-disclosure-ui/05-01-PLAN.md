---
phase: 05-manifest-audit-and-disclosure-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/manifest.json
  - docs/cws-justifications.md
autonomous: true
requirements:
  - MNFST-01
  - MNFST-02
  - MNFST-03
  - MNFST-04

must_haves:
  truths:
    - "Manifest version reads 1.1.0"
    - "unlimitedStorage permission is present in manifest.json"
    - "connect-src CSP retains raw.githubusercontent.com (grep-verified as used by WebLLM)"
    - "Manifest description frames the extension as a real-time creator dashboard with specific feature names"
    - "CSP justifications are documented explaining why each connect-src entry exists"
  artifacts:
    - path: "extension/manifest.json"
      provides: "CWS-ready manifest with correct version, permissions, description, and CSP"
      contains: "1.1.0"
    - path: "docs/cws-justifications.md"
      provides: "CSP rationale section for CWS reviewers and future maintainers"
      contains: "CSP"
  key_links:
    - from: "extension/manifest.json"
      to: "docs/cws-justifications.md"
      via: "CSP entries documented in justifications file"
      pattern: "connect-src"
---

<objective>
Audit and update manifest.json for Chrome Web Store submission readiness: bump version to 1.1.0, add unlimitedStorage permission, update description to single-purpose framing, and verify CSP entries are correct and documented.

Purpose: A clean, correctly versioned manifest with justified permissions is required before CWS submission. Each CSP entry must be grep-verified and documented so reviewers can understand why it exists.
Output: Updated manifest.json and expanded cws-justifications.md with CSP rationale section.
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-manifest-audit-and-disclosure-ui/05-CONTEXT.md
@.planning/phases/05-manifest-audit-and-disclosure-ui/05-RESEARCH.md
@extension/manifest.json
@docs/cws-justifications.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update manifest.json — version, permissions, description, and verify CSP</name>
  <files>extension/manifest.json</files>
  <action>
Edit extension/manifest.json with the following four changes:

1. **Version bump** (MNFST-02): Change `"version": "0.1.0"` to `"version": "1.1.0"`.

2. **Add unlimitedStorage** (MNFST-01): Add `"unlimitedStorage"` to the `"permissions"` array. Final array:
   ```json
   "permissions": [
     "sidePanel",
     "storage",
     "unlimitedStorage"
   ]
   ```

3. **Update description** (MNFST-04): Replace the current description with a single-purpose framing per user decision. Use:
   ```
   "description": "Real-time creator dashboard for YouTube and Twitch live chat. Clusters messages into questions, issues, and requests; tracks sentiment, trending topics, and session history. Optional AI summaries."
   ```
   This follows the locked decisions: frame as "creator dashboard", name specific features (message clusters, sentiment, trending topics, session history), name platforms (YouTube and Twitch), mention optional AI summaries.

4. **Verify CSP entries** (MNFST-03): Run grep to confirm `raw.githubusercontent.com` is used by the bundled WebLLM library:
   ```bash
   grep -r "raw.githubusercontent.com" extension/
   ```
   Research confirms it IS used at `extension/libs/web-llm/index.js:918` for `modelLibURLPrefix`. **Keep the entry.** Do not modify the CSP string — all three connect-src entries (`huggingface.co`, `cdn-lfs.huggingface.co`, `raw.githubusercontent.com`) are required for WebLLM functionality.

   Also verify `script-src 'self' 'wasm-unsafe-eval'` and `object-src 'self'` are the correct minimum for MV3 with WASM loading. Per Chrome docs, `'wasm-unsafe-eval'` is required for `WebAssembly.instantiate()` and is the correct directive (not `'unsafe-eval'`). No changes needed to script-src or object-src.

Do NOT add comments inside manifest.json — JSON does not support comments. CSP justifications go in docs/cws-justifications.md (Task 2).
  </action>
  <verify>
Run: `python3 -c "import json; json.load(open('extension/manifest.json'))"` — must parse without error.
Confirm version: `grep '"version"' extension/manifest.json` — must show `"1.1.0"`.
Confirm unlimitedStorage: `grep 'unlimitedStorage' extension/manifest.json` — must match.
Confirm description contains "creator dashboard": `grep 'creator dashboard' extension/manifest.json` — must match.
Confirm CSP entries preserved: `grep 'raw.githubusercontent.com' extension/manifest.json` — must match.
  </verify>
  <done>manifest.json has version 1.1.0, unlimitedStorage permission, single-purpose description, and all three connect-src CSP entries intact</done>
</task>

<task type="auto">
  <name>Task 2: Add CSP rationale section to cws-justifications.md</name>
  <files>docs/cws-justifications.md</files>
  <action>
Add a new section to `docs/cws-justifications.md` between the "Permission Justifications" section and the "Remote Code Declaration" section. Title it "## Content Security Policy (CSP) Rationale".

Document each CSP directive and why it exists:

**script-src 'self' 'wasm-unsafe-eval':**
- `'self'` — Extension scripts loaded from the extension package
- `'wasm-unsafe-eval'` — Required for WebAssembly.instantiate() used by both the Rust WASM analysis engine and WebLLM model inference

**object-src 'self':**
- Default minimum for MV3 extensions per Chrome documentation

**connect-src entries:**
- `https://huggingface.co` — WebLLM fetches model configuration and metadata from HuggingFace model hub
- `https://cdn-lfs.huggingface.co` — WebLLM downloads ONNX model weight files from HuggingFace's large file storage CDN
- `https://raw.githubusercontent.com` — WebLLM fetches WebGPU shader WASM files from the mlc-ai/binary-mlc-llm-libs GitHub repository (confirmed at `libs/web-llm/index.js:918` — `modelLibURLPrefix`)

Include a note: "All connect-src entries support the optional AI features. Without AI enabled, no external connections are made. Model weights are binary tensor data, not executable code."

Also update the unlimitedStorage justification to remove the Phase 5 note ("**Note: Added to manifest in Phase 5...**") since this phase IS Phase 5 and the permission will now be in the manifest.
  </action>
  <verify>
Confirm new section exists: `grep 'Content Security Policy' docs/cws-justifications.md` — must match.
Confirm Phase 5 note removed: `grep -c 'Phase 5' docs/cws-justifications.md` — should return 0.
Confirm all three connect-src domains documented: `grep -c 'huggingface\|githubusercontent' docs/cws-justifications.md` — should be at least 3.
  </verify>
  <done>cws-justifications.md has a CSP rationale section explaining every directive, all three connect-src domains are justified, and the Phase 5 note on unlimitedStorage is removed</done>
</task>

</tasks>

<verification>
1. `python3 -c "import json; d=json.load(open('extension/manifest.json')); assert d['version']=='1.1.0'; assert 'unlimitedStorage' in d['permissions']; assert 'creator dashboard' in d['description']; assert 'raw.githubusercontent.com' in d['content_security_policy']['extension_pages']"` — all assertions pass
2. `grep -c 'Phase 5' docs/cws-justifications.md` returns 0
3. `grep 'CSP' docs/cws-justifications.md` returns matches
</verification>

<success_criteria>
- manifest.json version is 1.1.0
- manifest.json permissions include unlimitedStorage
- manifest.json description uses single-purpose "creator dashboard" framing with feature names and platform names
- CSP connect-src retains all three domains (verified by grep that raw.githubusercontent.com is used by WebLLM)
- CSP rationale documented in cws-justifications.md with per-entry justifications
</success_criteria>

<output>
After completion, create `.planning/phases/05-manifest-audit-and-disclosure-ui/05-01-SUMMARY.md`
</output>
