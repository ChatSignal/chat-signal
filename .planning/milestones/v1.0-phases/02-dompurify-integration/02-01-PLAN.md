---
phase: 02-dompurify-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/libs/dompurify/purify.min.js
  - extension/sidebar/sidebar.html
  - extension/sidebar/utils/DOMHelpers.js
autonomous: true
requirements:
  - SAN-01
  - SAN-03

must_haves:
  truths:
    - "DOMPurify 3.3.1 is vendored at extension/libs/dompurify/purify.min.js"
    - "sidebar.html loads DOMPurify before the ES module script"
    - "safeSetHTML in DOMHelpers.js delegates to DOMPurify.sanitize() instead of regex patterns"
    - "The old regex-based safeSetHTML in DOMHelpers.js is completely removed"
  artifacts:
    - path: "extension/libs/dompurify/purify.min.js"
      provides: "Vendored DOMPurify 3.3.1 UMD build"
    - path: "extension/sidebar/sidebar.html"
      provides: "Script tag loading DOMPurify before ES module"
      contains: "libs/dompurify/purify.min.js"
    - path: "extension/sidebar/utils/DOMHelpers.js"
      provides: "DOMPurify-backed safeSetHTML and exported DOMPURIFY_CONFIG"
      exports: ["safeSetHTML", "DOMPURIFY_CONFIG"]
  key_links:
    - from: "extension/sidebar/sidebar.html"
      to: "extension/libs/dompurify/purify.min.js"
      via: "script tag with src='../libs/dompurify/purify.min.js'"
      pattern: "script src.*libs/dompurify/purify\\.min\\.js"
    - from: "extension/sidebar/utils/DOMHelpers.js"
      to: "window.DOMPurify"
      via: "DOMPurify.sanitize() call inside safeSetHTML"
      pattern: "DOMPurify\\.sanitize"
---

<objective>
Vendor DOMPurify 3.3.1 into the extension and replace the regex-based safeSetHTML in DOMHelpers.js with a DOMPurify-backed implementation.

Purpose: Establishes the sanitization foundation that Plan 02 will use to migrate all innerHTML sites in sidebar.js.
Output: Vendored library file, updated sidebar.html with script tag, rewritten DOMHelpers.js with DOMPURIFY_CONFIG export.
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dompurify-integration/02-CONTEXT.md
@.planning/phases/02-dompurify-integration/02-RESEARCH.md
@extension/sidebar/utils/DOMHelpers.js
@extension/sidebar/sidebar.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor DOMPurify and add script tag</name>
  <files>
    extension/libs/dompurify/purify.min.js
    extension/sidebar/sidebar.html
  </files>
  <action>
1. Download DOMPurify 3.3.1 UMD build:
   - `curl -o extension/libs/dompurify/purify.min.js https://unpkg.com/dompurify@3.3.1/dist/purify.min.js`
   - Create the `extension/libs/dompurify/` directory first if it does not exist.
   - Verify the downloaded file contains "DOMPurify" string and is non-empty.

2. Update `extension/sidebar/sidebar.html`:
   - At line 170 (currently `<script type="module" src="sidebar.js"></script>`), insert the DOMPurify script tag IMMEDIATELY BEFORE the existing module script:
     ```html
     <script src="../libs/dompurify/purify.min.js"></script>
     <script type="module" src="sidebar.js"></script>
     ```
   - The path `../libs/dompurify/purify.min.js` is correct because sidebar.html is in `extension/sidebar/` and the library is in `extension/libs/dompurify/`.
   - Do NOT add `defer`, `async`, or `type="module"` to the DOMPurify script tag. It must load synchronously before the module script.

No manifest.json changes needed — vendored local files are covered by the existing `script-src 'self'` CSP.
  </action>
  <verify>
    - `test -f extension/libs/dompurify/purify.min.js && echo "File exists"` returns "File exists"
    - `grep -c "DOMPurify" extension/libs/dompurify/purify.min.js` returns a positive number
    - `grep "libs/dompurify/purify.min.js" extension/sidebar/sidebar.html` shows the script tag
    - The DOMPurify script tag appears on the line BEFORE `<script type="module" src="sidebar.js">` in sidebar.html
  </verify>
  <done>
    purify.min.js exists at extension/libs/dompurify/purify.min.js, and sidebar.html loads it via a plain script tag before the ES module.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace safeSetHTML internals in DOMHelpers.js</name>
  <files>
    extension/sidebar/utils/DOMHelpers.js
  </files>
  <action>
Rewrite the `safeSetHTML` function and add exports in `extension/sidebar/utils/DOMHelpers.js`:

1. Add at the TOP of the file (after the existing module doc comment):
   ```javascript
   // All innerHTML must use DOMPurify

   // Centralized DOMPurify configuration — one place to tighten later
   // DOMPurify is loaded as a global via <script> tag in sidebar.html
   export const DOMPURIFY_CONFIG = {};
   ```

2. Replace the entire `safeSetHTML` function (currently lines 22-38, the regex-based implementation with safePatterns array) with:
   ```javascript
   // Safe HTML setter — DOMPurify-backed (replaces old regex implementation)
   export function safeSetHTML(element, htmlContent) {
     element.innerHTML = DOMPurify.sanitize(htmlContent, DOMPURIFY_CONFIG);
   }
   ```

3. The function signature stays the same: `(element, htmlContent)`. The export keyword is already present on the existing function — keep it.

4. Remove ALL traces of the old implementation:
   - Delete the `safePatterns` array
   - Delete the `isSafe` check
   - Delete the `console.error('Unsafe HTML blocked:')` fallback
   - Delete the `element.textContent = 'Content blocked for security'` fallback

5. Leave ALL other functions in DOMHelpers.js unchanged: `escapeHtml`, `safeCreateElement`, `createListItem`, `createSpan`, `createDiv`, `batchDOMUpdates`, `showElement`, `hideElement`, `toggleElement`, `showModal`, `hideModal`, `setActiveTab`, `setTheme`, `getSystemTheme`, `copyToClipboard`.

Key: Use bare `DOMPurify` (not `window.DOMPurify`) per research recommendation. If the global is not available the error will be immediately obvious.
  </action>
  <verify>
    - `grep "safePatterns" extension/sidebar/utils/DOMHelpers.js` returns NO matches (old regex gone)
    - `grep "DOMPurify.sanitize" extension/sidebar/utils/DOMHelpers.js` returns a match (new implementation present)
    - `grep "export const DOMPURIFY_CONFIG" extension/sidebar/utils/DOMHelpers.js` returns a match
    - `grep "Unsafe HTML blocked" extension/sidebar/utils/DOMHelpers.js` returns NO matches (old fallback gone)
    - `grep "Content blocked for security" extension/sidebar/utils/DOMHelpers.js` returns NO matches
  </verify>
  <done>
    DOMHelpers.js exports DOMPURIFY_CONFIG and a DOMPurify-backed safeSetHTML. No regex patterns remain. The function signature is unchanged so existing callers work without modification.
  </done>
</task>

</tasks>

<verification>
1. `extension/libs/dompurify/purify.min.js` exists and contains DOMPurify code
2. `extension/sidebar/sidebar.html` has DOMPurify script tag before the module script tag
3. `extension/sidebar/utils/DOMHelpers.js` exports `DOMPURIFY_CONFIG` and has DOMPurify-backed `safeSetHTML`
4. No regex patterns, no "Unsafe HTML blocked", no "Content blocked for security" remain in DOMHelpers.js
</verification>

<success_criteria>
- DOMPurify 3.3.1 is vendored and loadable from sidebar.html
- safeSetHTML delegates to DOMPurify.sanitize() with centralized config
- Old regex implementation is completely removed from DOMHelpers.js
- All other DOMHelpers.js functions are untouched
</success_criteria>

<output>
After completion, create `.planning/phases/02-dompurify-integration/02-01-SUMMARY.md`
</output>
