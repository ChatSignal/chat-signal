---
phase: 12-integration-and-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - extension/sidebar/sidebar.html
autonomous: true
requirements:
  - FBK-03

must_haves:
  truths:
    - "Consent modal text clearly distinguishes the encoder (~23MB, loads automatically) from the language model (~400MB, opt-in)"
    - "Storage space warning references ~400MB for the AI language model specifically"
    - "Storage space check threshold remains gated on Qwen's ~450MB (no change to JS logic, only display text changes)"
  artifacts:
    - path: "extension/sidebar/sidebar.html"
      provides: "Updated consent modal text separating encoder and language model disclosures, updated storage warning text"
      contains: "~23MB"
  key_links:
    - from: "extension/sidebar/sidebar.html"
      to: "extension/sidebar/sidebar.js"
      via: "consent modal shown by checkAISettings(), storage check gates Enable AI button"
      pattern: "llm-consent-modal"
---

<objective>
Update the consent modal to clearly disclose both models: the MiniLM encoder (~23MB, automatic) and the Qwen language model (~400MB, opt-in). Update the storage space warning text to reference the language model specifically.

Purpose: FBK-03 requires the consent modal to accurately represent what users are agreeing to. The current text only mentions "~450MB AI model" without distinguishing the automatic encoder from the opt-in language model.
Output: Updated sidebar.html with clear two-model disclosure and accurate storage warning.
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-integration-and-hardening/12-CONTEXT.md
@.planning/phases/12-integration-and-hardening/12-RESEARCH.md
@.planning/phases/12-integration-and-hardening/12-01-SUMMARY.md
@extension/sidebar/sidebar.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update consent modal disclosure text and storage warning</name>
  <files>extension/sidebar/sidebar.html</files>
  <action>
**Update the consent modal detail paragraph** (sidebar.html lines 93-97).

Replace the current `<p class="modal-detail">` content:
```html
<p class="modal-detail">
  All processing happens locally — no chat data leaves your browser.
  Enabling this will download a ~450MB AI model from HuggingFace on first use.
  The model is stored locally and persists across browser sessions.
</p>
```

With the updated two-model disclosure per CONTEXT.md locked decision ("Update consent modal text to mention both models: a small encoder (~23MB) loads automatically, enabling AI adds a ~450MB language model"):
```html
<p class="modal-detail">
  All processing happens locally — no chat data leaves your browser.
  A small encoder model (~23MB) loads automatically to power clustering.
  Enabling AI adds a language model (~400MB) downloaded from HuggingFace on first use.
  Both models are stored locally and persist across browser sessions.
</p>
```

Per research (12-RESEARCH.md): The ~950MB figure in REQUIREMENTS.md FBK-03 is overstated. The actual Qwen2.5-0.5B-Instruct-q4f16_1 download is ~400MB per extension/WEBLLM_SETUP.md. Combined with MiniLM (~23MB), total is ~423MB. Use ~400MB for the opt-in language model size (down from ~450MB) to match the authoritative source.

**Update the storage space warning** (sidebar.html lines 102-104).

Replace:
```html
<p id="llm-space-warning" class="modal-warning hidden">
  Not enough disk space available (~450MB needed)
</p>
```

With:
```html
<p id="llm-space-warning" class="modal-warning hidden">
  Not enough disk space available (~400MB needed for the AI language model)
</p>
```

**Do NOT change the JavaScript storage check threshold.** The existing `navigator.storage.estimate()` check in sidebar.js uses 450MB as the threshold (which includes buffer for IndexedDB overhead). Per CONTEXT.md locked decision: "Storage space check (navigator.storage.estimate) gates only on Qwen's ~450MB." The JS threshold stays at 450MB; only the display text changes.

**Do NOT touch the `<p>Enable AI-powered summaries and mood analysis?</p>` heading** — it's clear and accurate as-is.
  </action>
  <verify>
1. Grep for "~23MB" in sidebar.html — should find the encoder size mention in the consent modal.
2. Grep for "~400MB" in sidebar.html — should find the language model size in both the modal-detail and the llm-space-warning.
3. Grep for "loads automatically" in sidebar.html — should confirm the encoder auto-load disclosure.
4. Grep for "Both models are stored locally" in sidebar.html — should confirm persistence disclosure covers both models.
5. Verify the storage check threshold in sidebar.js is UNCHANGED: `grep "450" extension/sidebar/sidebar.js` — should still find the 450MB threshold value.
  </verify>
  <done>Consent modal clearly separates encoder (~23MB auto) from language model (~400MB opt-in). Storage warning references language model specifically. No changes to storage check threshold logic.</done>
</task>

</tasks>

<verification>
1. **FBK-03 consent text**: Open extension for the first time (or clear `aiConsentShown` flag). Consent modal should display: "A small encoder model (~23MB) loads automatically to power clustering. Enabling AI adds a language model (~400MB)..."
2. **FBK-03 storage warning**: Simulate low disk space. Warning should say "Not enough disk space available (~400MB needed for the AI language model)".
3. **No regression**: The Enable AI / Skip buttons still function correctly. Storage check threshold (450MB in JS) is unchanged.
</verification>

<success_criteria>
- Consent modal text mentions both models with correct sizes (~23MB encoder, ~400MB language model)
- Encoder disclosed as automatic, language model disclosed as opt-in
- Storage warning references language model specifically
- No JavaScript behavior changes (storage threshold, button logic, consent flow)
</success_criteria>

<output>
After completion, create `.planning/phases/12-integration-and-hardening/12-02-SUMMARY.md`
</output>
