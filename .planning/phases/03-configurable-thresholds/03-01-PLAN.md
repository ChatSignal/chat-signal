---
phase: 03-configurable-thresholds
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/options/options.html
  - extension/options/options.js
  - extension/sidebar/sidebar.js
  - extension/sidebar/modules/StateManager.js
  - extension/sidebar/modules/SessionManager.js
  - extension/sidebar/utils/ValidationHelpers.js
autonomous: true
requirements: [THR-01, THR-02]
must_haves:
  truths:
    - "Options page shows an Inactivity Timeout slider with range 30-600s, default 120s, step 30s"
    - "Changing the inactivity timeout slider displays raw seconds (e.g. '120s')"
    - "Sidebar inactivity detection uses the settings value instead of hardcoded 120000ms"
    - "SessionManager inactivity detection uses the settings value instead of stateManager.INACTIVITY_TIMEOUT"
    - "Both WASM analyze_chat_with_settings call sites pass settings.duplicateWindow (already wired, verified)"
  artifacts:
    - path: "extension/options/options.html"
      provides: "Inactivity timeout slider row in Spam Detection section"
      contains: "inactivity-timeout"
    - path: "extension/options/options.js"
      provides: "inactivityTimeout wiring in inputs, displays, getInputValues, setInputValues, updateDisplays, DEFAULT_SETTINGS"
      contains: "inactivityTimeout"
    - path: "extension/sidebar/sidebar.js"
      provides: "Live settings read replacing INACTIVITY_TIMEOUT constant"
      contains: "settings.inactivityTimeout"
    - path: "extension/sidebar/modules/StateManager.js"
      provides: "inactivityTimeout in DEFAULT_SETTINGS, INACTIVITY_TIMEOUT constant removed"
      contains: "inactivityTimeout: 120"
    - path: "extension/sidebar/modules/SessionManager.js"
      provides: "Live settings read replacing stateManager.INACTIVITY_TIMEOUT"
      contains: "stateManager.settings.inactivityTimeout"
    - path: "extension/sidebar/utils/ValidationHelpers.js"
      provides: "inactivityTimeout validation rule in validateSettings"
      contains: "inactivityTimeout"
  key_links:
    - from: "extension/options/options.js"
      to: "chrome.storage.sync"
      via: "saveSettings writes inactivityTimeout"
      pattern: "inactivityTimeout.*parseInt"
    - from: "extension/sidebar/sidebar.js"
      to: "settings.inactivityTimeout"
      via: "startInactivityCheck reads live value"
      pattern: "settings\\.inactivityTimeout"
    - from: "extension/sidebar/modules/SessionManager.js"
      to: "stateManager.settings.inactivityTimeout"
      via: "startInactivityCheck reads from stateManager.settings"
      pattern: "stateManager\\.settings\\.inactivityTimeout"
---

<objective>
Add inactivity timeout as a user-configurable setting and verify duplicateWindow wiring.

Purpose: Users currently cannot tune when the "stream ended?" prompt appears — it fires after a hardcoded 2 minutes. Ad breaks or bathroom breaks trigger false prompts. Exposing inactivityTimeout lets users set 30-600 seconds based on their stream. The duplicateWindow setting is already wired to WASM but needs confirmation.

Output: Inactivity timeout slider in options page, three DEFAULT_SETTINGS copies updated, two hardcoded constants replaced with live settings reads, duplicateWindow wiring verified.
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-configurable-thresholds/03-RESEARCH.md
@extension/options/options.html
@extension/options/options.js
@extension/sidebar/sidebar.js
@extension/sidebar/modules/StateManager.js
@extension/sidebar/modules/SessionManager.js
@extension/sidebar/utils/ValidationHelpers.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add inactivityTimeout to all DEFAULT_SETTINGS and options UI</name>
  <files>
    extension/options/options.html
    extension/options/options.js
    extension/sidebar/sidebar.js
    extension/sidebar/modules/StateManager.js
  </files>
  <action>
**1. Add `inactivityTimeout: 120` to all three DEFAULT_SETTINGS copies:**

- `extension/sidebar/sidebar.js` line 15-23 DEFAULT_SETTINGS object: add `inactivityTimeout: 120` (place after `analysisWindowSize: 500`)
- `extension/sidebar/modules/StateManager.js` line 271-278 DEFAULT_SETTINGS object: add `inactivityTimeout: 120` (place after `analysisWindowSize: 500`)
- `extension/options/options.js` line 3-11 DEFAULT_SETTINGS object: add `inactivityTimeout: 120` (place after `analysisWindowSize: 500`)

**2. Add slider row to options.html** — inside the "Spam Detection" section (after the duplicate-window setting-row, before the closing `</section>` tag on line 73):

```html
<div class="setting-row">
  <label for="inactivity-timeout">
    <span class="setting-name">Inactivity timeout</span>
    <span class="setting-description">Seconds without new messages before showing "stream ended?" prompt</span>
  </label>
  <div class="input-group">
    <input type="range" id="inactivity-timeout" min="30" max="600" step="30" value="120">
    <span class="value-display" id="inactivity-timeout-value">120s</span>
  </div>
</div>
```

**3. Wire options.js** — add inactivityTimeout to the existing pipeline objects:

- `inputs` object (line 19-27): add `inactivityTimeout: document.getElementById('inactivity-timeout')`
- `displays` object (line 30-36): add `inactivityTimeout: document.getElementById('inactivity-timeout-value')`
- `updateDisplays` function: add `displays.inactivityTimeout.textContent = \`${settings.inactivityTimeout}s\`;`
- `setInputValues` function: add `inputs.inactivityTimeout.value = settings.inactivityTimeout;`
- `getInputValues` function: add `inactivityTimeout: parseInt(inputs.inactivityTimeout.value, 10)` to the returned object

Per user decision: display raw seconds (e.g., "120s") — not human-readable minutes.
  </action>
  <verify>
Search all three DEFAULT_SETTINGS for `inactivityTimeout: 120` — must appear in all three files. Search options.html for `id="inactivity-timeout"` — must have min="30" max="600" step="30" value="120". Search options.js for `inactivityTimeout` — must appear in inputs, displays, updateDisplays, setInputValues, getInputValues.
  </verify>
  <done>
All three DEFAULT_SETTINGS copies include `inactivityTimeout: 120`. Options page has a range slider with correct min/max/step. The full options.js pipeline (inputs, displays, updateDisplays, setInputValues, getInputValues) handles inactivityTimeout. Value display shows raw seconds with "s" suffix.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded INACTIVITY_TIMEOUT constants with live settings reads</name>
  <files>
    extension/sidebar/sidebar.js
    extension/sidebar/modules/StateManager.js
    extension/sidebar/modules/SessionManager.js
    extension/sidebar/utils/ValidationHelpers.js
  </files>
  <action>
**1. sidebar.js — Remove hardcoded constant, use live settings:**

- Delete line 114: `const INACTIVITY_TIMEOUT = 120000; // 2 minutes`
- In `startInactivityCheck()` function (line 939-954), change line 948 from:
  `if (timeSinceLastMessage >= INACTIVITY_TIMEOUT) {`
  to:
  ```javascript
  const inactivityMs = (settings.inactivityTimeout || 120) * 1000;
  if (timeSinceLastMessage >= inactivityMs) {
  ```
  The `|| 120` fallback guards against undefined during startup race before settings load. The `* 1000` converts the seconds value stored in settings to milliseconds for Date.now() comparison.

**2. StateManager.js — Remove INACTIVITY_TIMEOUT property:**

- Delete line 53: `this.INACTIVITY_TIMEOUT = 120000; // 2 minutes`
  (The `settings` property on StateManager already holds the live `inactivityTimeout` value via the settings getter on line 60.)

**3. SessionManager.js — Read from stateManager.settings instead of stateManager.INACTIVITY_TIMEOUT:**

- In `startInactivityCheck()` method (line 128-145), change line 137 from:
  `if (timeSinceLastMessage >= stateManager.INACTIVITY_TIMEOUT) {`
  to:
  ```javascript
  const inactivityMs = (stateManager.settings.inactivityTimeout || 120) * 1000;
  if (timeSinceLastMessage >= inactivityMs) {
  ```
  Same pattern as sidebar.js: fallback to 120s, convert to ms.

**4. ValidationHelpers.js — Add inactivityTimeout validation:**

- In `validateSettings()` function, after the `analysisWindowSize` validation block (after line 132), add with the same undefined-guard pattern used by analysisWindowSize:
  ```javascript
  // Validate inactivityTimeout
  if (settings.inactivityTimeout !== undefined) {
    if (!Number.isFinite(settings.inactivityTimeout) || settings.inactivityTimeout < 30 || settings.inactivityTimeout > 600) {
      throw new Error('inactivityTimeout must be a number between 30 and 600 seconds');
    }
  }
  ```
  The undefined guard tolerates settings objects saved before Phase 3 (same pattern as analysisWindowSize).

**5. Verify duplicateWindow wiring (THR-02):**

Read sidebar.js lines 307-312 and 681-686. Confirm both `analyze_chat_with_settings` call sites pass `settings.duplicateWindow * 1000`. No code changes needed — the wiring is already correct. Document in the summary that THR-02 was pre-existing.

Per user decision: change takes effect immediately — the sidebar.js `startInactivityCheck()` is called on every message batch (line ~670), so the new settings value is picked up on the next batch without explicit timer restart. SessionManager follows the same pattern.
  </action>
  <verify>
1. Search sidebar.js for `INACTIVITY_TIMEOUT` — must return zero results (constant removed).
2. Search sidebar.js for `settings.inactivityTimeout` — must appear in `startInactivityCheck()`.
3. Search StateManager.js for `INACTIVITY_TIMEOUT` — must return zero results (property removed).
4. Search SessionManager.js for `stateManager.INACTIVITY_TIMEOUT` — must return zero results.
5. Search SessionManager.js for `stateManager.settings.inactivityTimeout` — must appear in `startInactivityCheck()`.
6. Search ValidationHelpers.js for `inactivityTimeout` — must appear with `Number.isFinite` guard.
7. Confirm sidebar.js still has `settings.duplicateWindow * 1000` at both analyze_chat_with_settings call sites.
8. Run `cd wasm-engine && cargo test` — all 18 tests pass (no Rust changes, but confirms nothing broke).
  </verify>
  <done>
Both hardcoded INACTIVITY_TIMEOUT constants are removed. sidebar.js and SessionManager.js both read `settings.inactivityTimeout` (or `stateManager.settings.inactivityTimeout`) with `|| 120` fallback and `* 1000` ms conversion. ValidationHelpers validates inactivityTimeout with undefined guard. duplicateWindow wiring confirmed at both WASM call sites (no changes needed). All existing tests pass.
  </done>
</task>

</tasks>

<verification>
1. All three DEFAULT_SETTINGS copies agree: `inactivityTimeout: 120`
2. No remaining `INACTIVITY_TIMEOUT` constant in sidebar.js or StateManager.js
3. No remaining `stateManager.INACTIVITY_TIMEOUT` in SessionManager.js
4. options.html has inactivity-timeout slider with correct attributes
5. options.js full pipeline handles inactivityTimeout
6. ValidationHelpers.js validates inactivityTimeout with Number.isFinite
7. Both analyze_chat_with_settings call sites pass settings.duplicateWindow * 1000
8. `cd wasm-engine && cargo test` passes
</verification>

<success_criteria>
- Options page has an Inactivity Timeout slider (30-600s, default 120s, step 30s) displaying raw seconds
- Both sidebar.js and SessionManager.js read inactivityTimeout from live settings, not hardcoded constants
- The INACTIVITY_TIMEOUT constant is fully removed from the codebase
- duplicateWindow is confirmed wired to both WASM call sites (no change needed)
- Existing Rust tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/03-configurable-thresholds/03-01-SUMMARY.md`
</output>
