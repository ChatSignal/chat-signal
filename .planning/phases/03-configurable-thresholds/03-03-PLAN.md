---
phase: 03-configurable-thresholds
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - extension/sidebar/sidebar.js
autonomous: true
gap_closure: true
requirements: [THR-03]

must_haves:
  truths:
    - "All numeric threshold validation in sidebar.js rejects NaN inputs"
    - "sidebar.js validateAnalysisResult() uses Number.isFinite() for bucket.count, topic.count, and processed_count"
    - "sidebar.js validateMessages() uses Number.isFinite() for msg.timestamp"
  artifacts:
    - path: "extension/sidebar/sidebar.js"
      provides: "NaN-safe numeric validation in local validateAnalysisResult and validateMessages"
      contains: "Number.isFinite"
  key_links:
    - from: "extension/sidebar/sidebar.js:validateAnalysisResult"
      to: "Number.isFinite"
      via: "direct call replacing typeof check"
      pattern: "!Number\\.isFinite\\(bucket\\.count\\)"
    - from: "extension/sidebar/sidebar.js:validateMessages"
      to: "Number.isFinite"
      via: "direct call replacing typeof check"
      pattern: "!Number\\.isFinite\\(msg\\.timestamp\\)"
---

<objective>
Replace the four remaining `typeof x !== 'number'` checks in sidebar.js's local `validateAnalysisResult()` and `validateMessages()` functions with `!Number.isFinite(x)` to close the last THR-03 verification gap.

Purpose: sidebar.js lines 586, 611, 626, 645 use `typeof x !== 'number'` which silently accepts NaN (since `typeof NaN === 'number'`). These are the actual runtime validation path for WASM output — the hardened ValidationHelpers.js versions are never called from sidebar.js. This gap was found during phase verification (03-VERIFICATION.md, truth 8).

Output: sidebar.js with all four typeof-number checks replaced by Number.isFinite guards.
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-configurable-thresholds/03-VERIFICATION.md
@extension/sidebar/sidebar.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace typeof number checks with Number.isFinite() in sidebar.js local validation functions</name>
  <files>extension/sidebar/sidebar.js</files>
  <action>
In `extension/sidebar/sidebar.js`, make exactly four replacements in the local `validateAnalysisResult()` and `validateMessages()` functions (lines 570-654):

1. Line 586 — inside validateAnalysisResult(), bucket validation:
   Replace: `typeof bucket.count !== 'number' || bucket.count < 0`
   With: `!Number.isFinite(bucket.count) || bucket.count < 0`

2. Line 611 — inside validateAnalysisResult(), topic validation:
   Replace: `typeof topic.count !== 'number' || topic.count <= 0`
   With: `!Number.isFinite(topic.count) || topic.count <= 0`

3. Line 626 — inside validateAnalysisResult(), processed_count validation:
   Replace: `typeof result.processed_count !== 'number' || result.processed_count < 0`
   With: `!Number.isFinite(result.processed_count) || result.processed_count < 0`

4. Line 645 — inside validateMessages(), timestamp validation:
   Replace: `typeof msg.timestamp !== 'number' || msg.timestamp <= 0`
   With: `!Number.isFinite(msg.timestamp) || msg.timestamp <= 0`

Do NOT change any other code. Do NOT refactor to import from ValidationHelpers.js — that would be a larger change outside gap closure scope. The fix is purely swapping the guard expression.

Per user decision [03-02]: Number.isFinite() replaces typeof x !== 'number' everywhere — typeof NaN === 'number' is true, old guards silently accepted NaN.
  </action>
  <verify>
Run: `grep -n "typeof.*!== 'number'" extension/sidebar/sidebar.js` — must return zero matches.
Run: `grep -n "Number.isFinite" extension/sidebar/sidebar.js` — must show at least 4 matches in the validateAnalysisResult and validateMessages functions (lines ~570-654).
Run: `cd wasm-engine && cargo test` — all 18 tests pass (no Rust changes, sanity check).
  </verify>
  <done>
All four typeof-number checks in sidebar.js local validation functions replaced with Number.isFinite(). Zero `typeof.*!== 'number'` matches remain in sidebar.js. Phase goal "all numeric threshold validation rejects NaN inputs" is fully achieved across ValidationHelpers.js, options.js, AND sidebar.js.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "typeof.*!== 'number'" extension/sidebar/sidebar.js` returns zero matches
2. `grep -rn "typeof.*!== 'number'" extension/sidebar/utils/ValidationHelpers.js` returns zero matches (already clean from 03-02)
3. `grep -rn "typeof.*!== 'number'" extension/options/options.js` returns zero matches (already clean from 03-02)
4. `grep -c "Number.isFinite" extension/sidebar/sidebar.js` returns 4 or more
5. `cd wasm-engine && cargo test` — all 18 tests pass
</verification>

<success_criteria>
- Zero `typeof x !== 'number'` patterns remain in any validation function across the entire extension
- sidebar.js validateAnalysisResult() rejects NaN for bucket.count, topic.count, and processed_count
- sidebar.js validateMessages() rejects NaN for msg.timestamp
- All existing tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/03-configurable-thresholds/03-03-SUMMARY.md`
</output>
