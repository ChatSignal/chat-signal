---
phase: 04-privacy-and-dashboard-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/CNAME
  - docs/privacy-policy.md
  - docs/cws-justifications.md
  - PRIVACY.md
autonomous: true
requirements:
  - PRIV-01
  - PRIV-02
  - PRIV-03

must_haves:
  truths:
    - "Privacy policy file exists in docs/ with correct Jekyll front matter and permalink /privacy-policy"
    - "CNAME file exists in docs/ containing exactly 'chatsignal.dev' (single line, no protocol)"
    - "Privacy policy discloses all four required topics: DOM reading, chrome.storage.local, optional HuggingFace download, no external server transmission"
    - "CWS justifications doc has per-permission justifications for sidePanel, storage, unlimitedStorage, and both host_permissions"
    - "CWS justifications doc has data certification checkbox answers for website content and browsing activity"
    - "Root PRIVACY.md replaced with a pointer to the docs/ location"
  artifacts:
    - path: "docs/CNAME"
      provides: "GitHub Pages custom domain mapping"
      contains: "chatsignal.dev"
    - path: "docs/privacy-policy.md"
      provides: "Published privacy policy for CWS submission"
      contains: "permalink: /privacy-policy"
    - path: "docs/cws-justifications.md"
      provides: "CWS dashboard copy-paste reference for Privacy Practices tab"
      contains: "sidePanel"
  key_links:
    - from: "docs/privacy-policy.md"
      to: "CWS dashboard Privacy Policy URL field"
      via: "GitHub Pages at chatsignal.dev/privacy-policy"
      pattern: "permalink: /privacy-policy"
    - from: "docs/cws-justifications.md"
      to: "CWS dashboard Privacy Practices tab"
      via: "Manual copy-paste by user"
      pattern: "Permission Justifications"
---

<objective>
Write the privacy policy, CNAME file, and CWS dashboard justifications document in the docs/ directory, and replace the root PRIVACY.md with a pointer.

Purpose: Create all version-controlled compliance documents needed for CWS submission. The privacy policy becomes the live page at chatsignal.dev/privacy-policy once GitHub Pages is configured. The justifications doc is the copy-paste source for the CWS dashboard Privacy Practices tab.

Output: docs/CNAME, docs/privacy-policy.md, docs/cws-justifications.md, updated PRIVACY.md
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-privacy-and-dashboard-compliance/04-CONTEXT.md
@.planning/phases/04-privacy-and-dashboard-compliance/04-RESEARCH.md
@PRIVACY.md
@extension/manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/ directory with CNAME and privacy policy</name>
  <files>docs/CNAME, docs/privacy-policy.md</files>
  <action>
Create the `docs/` directory and two files:

**docs/CNAME** — Single line, no whitespace, no protocol:
```
chatsignal.dev
```

**docs/privacy-policy.md** — Rewrite the existing PRIVACY.md (at project root) in plain-language conversational tone per user decisions. Must include:

1. Jekyll front matter:
   ```yaml
   ---
   title: Privacy Policy
   permalink: /privacy-policy
   ---
   ```

2. Title: "Privacy Policy for Chat Signal Radar"
3. "Last updated: February 2026" (no version number, per user decision)

4. Required disclosure topics (PRIV-01):
   - **Chat DOM reading**: Use "processes" framing. Chat messages are already publicly visible. Processed locally in the browser, never sent to any server. Mention both YouTube and Twitch by name.
   - **chrome.storage.local**: Session summaries saved locally. Include clearing instructions: "You can clear them from the History tab or by removing the extension." (exact user decision wording)
   - **Optional HuggingFace download**: Inline with other data practices (NOT a separate section, per user decision). ~400MB model from HuggingFace CDN, one-time download, stored locally. No chat content sent to HuggingFace.
   - **No external server**: "Data never leaves your browser" mentioned naturally in the data practices flow, not prominently highlighted (per user decision). If AI disabled, zero external requests.

5. Tone rules (per locked decisions):
   - Conversational, easy to read, no legal jargon
   - "Chat Signal Radar does not collect personal information" (NOT "We do not collect")
   - Small indie extension feel, not corporate
   - Link to GitHub Issues (https://github.com/johnzilla/chat-signal-radar/issues) for privacy questions — no personal email exposed
   - Simple change notification: "We may update this policy. Check back for changes."

6. Do NOT include: version numbers, tables (too formal for the decided tone), children's privacy section (not required by CWS, was in old draft), "Summary" section at end (the whole policy should read as a summary).

Reference the existing PRIVACY.md for factual content (what data is accessed, storage locations, etc.) but completely rewrite the prose.
  </action>
  <verify>
- `cat docs/CNAME` shows exactly `chatsignal.dev` with no extra whitespace
- `head -5 docs/privacy-policy.md` shows correct Jekyll front matter with `permalink: /privacy-policy`
- `grep -c "chatsignal.dev/privacy-policy" docs/privacy-policy.md` returns 0 (the permalink is in front matter, not body)
- `grep -i "we do not collect" docs/privacy-policy.md` returns 0 (no legal-sounding phrasing)
- `grep -i "huggingface" docs/privacy-policy.md` returns at least 1 match (HuggingFace disclosure present)
- `grep -i "chrome.storage" docs/privacy-policy.md` OR `grep -i "locally" docs/privacy-policy.md` returns matches (storage disclosure present)
- `grep "github.com/johnzilla/chat-signal-radar/issues" docs/privacy-policy.md` returns 1 match (contact link present)
  </verify>
  <done>
docs/CNAME contains exactly "chatsignal.dev". docs/privacy-policy.md has correct Jekyll front matter, plain-language conversational tone, and discloses all four required topics (DOM reading, chrome.storage.local, HuggingFace download, no external server). No legal jargon. Contact links to GitHub Issues.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CWS justifications doc and update root PRIVACY.md</name>
  <files>docs/cws-justifications.md, PRIVACY.md</files>
  <action>
**docs/cws-justifications.md** — Single doc combining permission justifications AND data certification checkbox answers (per user decision). Structure to match CWS dashboard fields for easy copy-paste:

1. **Single Purpose Description** — One sentence: "Chat Signal Radar analyzes YouTube and Twitch live chat in real-time, clustering messages into categories, detecting trending topics, and tracking chat sentiment."

2. **Permission Justifications** (PRIV-02) — User-benefit focused framing (per locked decision). One section per permission:
   - **sidePanel**: "Shows the real-time chat analysis dashboard alongside the stream page so creators can monitor chat while watching."
   - **storage**: "Saves user settings (analysis preferences, AI consent choice) and session summaries to your local browser. All data stays on your device."
   - **unlimitedStorage**: "Stores the optional AI model (~400MB) in IndexedDB after a one-time download. The standard storage quota is too small for model weights this size." Mark clearly as: "Note: Added to manifest in Phase 5 — paste into dashboard after manifest update."
   - **host_permissions: youtube.com**: "Reads live chat messages from the YouTube page DOM to perform real-time message clustering, topic detection, and sentiment analysis. No data is transmitted off-device." (exact research draft wording per user decision on DOM reading framing)
   - **host_permissions: twitch.tv**: Same as youtube.com but for Twitch.

3. **Remote Code Declaration** — "No, I am not using remote code." With note: "HuggingFace download is model weights (ONNX tensors), not executable code. Manifest V3 prohibits remote code execution."

4. **Data Usage Checkboxes** (PRIV-03):
   - Group 1 (data types collected): Check "Website content" and "Browsing activity". Explicitly note which are NOT checked and why.
   - Group 2 (compliance certifications): Check all four limited-use statements. List each one with a brief justification.

5. **Privacy Policy URL**: `https://chatsignal.dev/privacy-policy`

**PRIVACY.md** (root) — Replace entire contents with a short pointer:
```markdown
# Privacy Policy

The published privacy policy for Chat Signal Radar is hosted at:

**https://chatsignal.dev/privacy-policy**

Source: [docs/privacy-policy.md](docs/privacy-policy.md)
```
This preserves the file (GitHub renders it on the repo page) but redirects to the authoritative source.
  </action>
  <verify>
- `grep "sidePanel" docs/cws-justifications.md` returns a match
- `grep "storage" docs/cws-justifications.md` returns matches
- `grep "unlimitedStorage" docs/cws-justifications.md` returns a match
- `grep "youtube.com" docs/cws-justifications.md` returns a match
- `grep "twitch.tv" docs/cws-justifications.md` returns a match
- `grep "Website content" docs/cws-justifications.md` returns a match (PRIV-03)
- `grep "Browsing activity" docs/cws-justifications.md` returns a match (PRIV-03)
- `grep "chatsignal.dev/privacy-policy" docs/cws-justifications.md` returns a match
- `grep "chatsignal.dev/privacy-policy" PRIVACY.md` returns a match (root file points to hosted URL)
- `wc -l PRIVACY.md` shows a small file (under 10 lines — it's just a pointer now)
  </verify>
  <done>
docs/cws-justifications.md has all five CWS dashboard sections filled: single purpose description, per-permission justifications (including unlimitedStorage marked for Phase 5), remote code declaration, data usage checkboxes with both groups, and privacy policy URL. Root PRIVACY.md is a short pointer to the hosted privacy policy URL.
  </done>
</task>

</tasks>

<verification>
1. `ls docs/` shows three files: CNAME, privacy-policy.md, cws-justifications.md
2. Privacy policy content covers all PRIV-01 disclosures (DOM, storage, HuggingFace, no-server)
3. CWS justifications cover all PRIV-02 permissions (sidePanel, storage, unlimitedStorage, youtube.com, twitch.tv)
4. Data certification covers PRIV-03 checkboxes (website content, browsing activity, limited-use compliance)
5. Consistency check: privacy policy disclosures and cws-justifications data categories refer to the same facts
6. Root PRIVACY.md points to chatsignal.dev/privacy-policy
</verification>

<success_criteria>
- docs/ directory contains CNAME, privacy-policy.md, and cws-justifications.md
- Privacy policy is plain-language, conversational, and discloses all four required data practices
- CWS justifications doc is structured for easy copy-paste into the CWS dashboard
- All three requirement IDs (PRIV-01, PRIV-02, PRIV-03) are fully addressed by the file contents
- Root PRIVACY.md redirects to the authoritative hosted version
</success_criteria>

<output>
After completion, create `.planning/phases/04-privacy-and-dashboard-compliance/04-01-SUMMARY.md`
</output>
